export default function DonatePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--color-background)]">
      <div className="px-6 text-center">
        <h1 className="text-xl font-semibold text-[var(--color-foreground)]">
          Donate
        </h1>
        <p className="mt-3 text-sm text-[var(--color-foreground)]/70">
          The donate experience is temporarily disabled while you refactor. This
          placeholder page exists so <code>/donate</code> returns 200 instead of
          404.
        </p>
      </div>
    </main>
  );
}

